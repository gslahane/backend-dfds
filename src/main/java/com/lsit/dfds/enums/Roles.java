package com.lsit.dfds.enums;

public enum Roles {
	STATE_ADMIN, STATE_CHECKER, STATE_MAKER, DIVISION_ADMIN, DISTRICT_ADMIN, DISTRICT_CHECKER, DISTRICT_MAKER, IA_ADMIN,
	MLA, MLC, HADP_ADMIN, // (new) for HADP login if needed
	MLA_REP // (optional) representative of MLA
}
