package com.lsit.dfds.enums;

//com.lsit.dfds.enums.Roles
public enum Roles {
	STATE_ADMIN, STATE_CHECKER, STATE_MAKER, DIVISION_ADMIN,

// District leadership + workflow roles
	DISTRICT_COLLECTOR, DISTRICT_DPO, DISTRICT_ADPO, DISTRICT_ADMIN, DISTRICT_CHECKER, DISTRICT_MAKER,

	IA_ADMIN,

// Representatives
	MLA, MLC, HADP_ADMIN, MLA_REP,

// Optional (login for vendors, if you decide to enable later)
	VENDOR
}
